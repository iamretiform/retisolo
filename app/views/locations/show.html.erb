<p class="callout" id="notice">
  <%= notice %>
    <p><strong>Latitude:</strong>
      <%= @location.latitude %>
    </p>
    <p><strong>Longitude:</strong>
      <%= @location.longitude %>
    </p>
    <h4>Address</h4>
    <p>
      <%= @location.address %>
    </p>
    Lat:
    <div class="callout" id="latr">
    </div>
    Long:
    <div class="callout" id="longr">
    </div>
    <div class="panel panel-default">
      <% if @location.sounds %>
        <div class="panel-body" id="sounds">
          <%= render template: 'sounds/show', collection: @location.sounds %>
        </div>
      <%else%>
        <div class="panel-body hidden" id="sounds">
        </div>
      <%end%>
    </div>
      <%= render 'sounds/sound_form', locals: { location_id: @location.id } %>
        <%= render 'google_map', location: @location %>
          <table class="table table-sm table-hover">
            <thead>
              <h4>Locations</h4>
              <tr>
                <th>Address</th>
                <th>Distance</th>
              </tr>
            </thead>
            <tbody>
              <% for location in @location.nearbys(20) %>
                <tr>
                  <td>
                    <%= link_to location.address, location %>
                  </td>
                  <td>(
                    <%= location.distance %> miles )
                      <% end %>
                  </td>
                </tr>
            </tbody>
          </table>
          </div>
          </li>
          </ul>
          <%= link_to 'Edit', edit_location_path(@location) %> |
            <%= link_to 'Back', locations_path %>
